name: flutter_google_places_api
description: This is dart implementation of google places api. Each requests returns correspoding response object.
version: 0.0.14
homepage: https://github.com/bamnenim/flutter_google_places_api

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  equatable: ^2.0.0
  meta: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
